
<h1>Listing Breweries</h1>

<p> Number of active breweries: <%= @active_breweries.count %> </p>

<p><%= render 'list', breweries: @active_breweries.count %></p>

<h2>Retired</h2>

<p><%= render 'list', breweries: @retired_breweries.count %></p>

<%= link_to('New Brewery', new_brewery_path, class:'btn btn-primary') if current_user %><br>
<%= link_to 'List of Beers', beers_path, class:'btn btn-info' %>
